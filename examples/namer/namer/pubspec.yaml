name: namer
description: "Key Namer - Test app for autoconnect functionality"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  dogpaw:
    path: ../../../packages/dogpaw
  dogpaw_widgets:
    path: ../../../packages/dogpaw_widgets
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
flutter:
  uses-material-design: true
